{
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "targets": {
    "deploy": {
      "executor": "nx-github-pages:deploy",
      "inputs": ["{workspaceRoot}/dist/page/**/*"],
      "dependsOn": [
        {
          "target": "build",
          "projects": ["slide-se109", "slide-se332"]
        }
      ],
      "options": {
        "directory": "{workspaceRoot}/dist/page/"
      },
      "cache": true
    }
  }
}
