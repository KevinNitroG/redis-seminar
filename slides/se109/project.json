{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "build": {
      "inputs": ["slidev"],
      "outputs": [
        "{projectRoot}/dist/",
        "{workspaceRoot}/dist/page/slide-se109/"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run build",
        "cwd": "{projectRoot}"
      },
      "configurations": {
        "ci": {
          "args": [
            "--base=/redis-seminar/slide-se109/",
            "--out=../../dist/page/slide-se109/"
          ]
        }
      }
    },
    "export": {
      "inputs": ["slidev"],
      "outputs": [
        "{projectRoot}/dist/",
        "{workspaceRoot}/dist/release/slide-se109.{pdf}"
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run export",
        "cwd": "{projectRoot}"
      },
      "configurations": {
        "ci": {
          "args": ["--format=pdf", "--output=../../dist/release/slide-se109"]
        }
      }
    }
  }
}
